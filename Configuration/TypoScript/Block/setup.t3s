lib.logo = TEXT
lib.logo {
	value < sitetitle
  wrap = <div id="logo">|</div>

	typolink {
		ATagBeforeWrap = 1
		wrap = <img src="{$folders.images}/go.svg" alt="|" />
		parameter = {$pages.home}
	}
}

lib.content < styles.content.get